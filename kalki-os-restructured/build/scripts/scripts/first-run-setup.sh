#!/bin/bash
# First run setup for Kalki OS (OOBE)
set -e

# Remove autostart entry after first run
rm -f "${HOME}/.config/autostart/kalki-first-run.desktop"

# Branding setup (placeholder for actual branding logic)
echo "Welcome to Kalki OS! Running first-time setup..."
# Add any additional OOBE steps here

exit 0 